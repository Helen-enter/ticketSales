export interface IOrder {
  age: string | null,
  birthDay: string | null,
  cardNumber: string,
  tourId: string | null,
  userId: string | null,


  // __v: number | never
  // _id: string | null
}
